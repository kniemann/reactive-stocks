server:
  port: 8080
management:
  endpoints:
    web:
      expose: '*'
logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.springframework.security: INFO
database: reactive-stocks